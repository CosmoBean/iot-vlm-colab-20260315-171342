# Teaching Flow

- Start with what a VLM is (image + text in, text out).
- Walk through install, setup, model loading, helper function, then UI.
- Run single-image example.
- Run multi-image example.
- Discuss where the model can fail.
